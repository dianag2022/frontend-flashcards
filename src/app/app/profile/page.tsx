"use client";

import { ProfileContent } from "@/components/auth/ProfileContent";

export default function AppProfilePage() {
  return <ProfileContent backHref="/app" backLabel="Volver al inicio" />;
}
